import { ReactNode } from "react";
interface AnimatedSectionProps {
    children: ReactNode;
    className?: string;
}
declare const AnimatedSection: ({ children, className, }: AnimatedSectionProps) => import("react/jsx-runtime").JSX.Element;
export default AnimatedSection;
